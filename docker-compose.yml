
services:
  rag-api:
    build:
      context: .
      dockerfile: apps/rag/Dockerfile
    container_name: rag-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 3
  
  agent:
    build:
      context: .
      dockerfile: Dockerfile.agent
    container_name: agent
    env_file:
      - .env
    environment:
      - RAG_API_URL=http://rag-api:8000/query
    depends_on:
      - rag-api
    stdin_open: true
    tty: true

  mcp-api:
    build:
      context: .
    command: uvicorn apps.mcp.mcp_api:app --host 0.0.0.0 --port 8001
    container_name: mcp-api
    env_file:
      - .env
    ports:
      - "8001:8001"
    depends_on:
      - rag-api
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 10s
      timeout: 5s
      retries: 3