# Vectorstore

This directory holds local Chroma vector databases.

These files are NOT committed to git to avoid repo bloat.

To rebuild:

1) Run chunking script in scripts/
2) Run embedding script in scripts/ 
